import java.io.FileWriter;
import java.io.IOException;
import java.io.PrintWriter;
import java.util.Locale;

public class Ej3 {
/*
    public static void listado(ArrayList<DatosEstadisticos> lista,String nombreFichero) throws PocosDatos{
        Locale.setDefault(new Locale("es"));
        if(lista == null || lista.size() <= 1){
          //  throw new PocosDatos();
        }else{
            try (PrintWriter out=new PrintWriter(new FileWriter(nombreFichero))) {

                out.println("NombreSerie            NumDatos          ValorMedio           Maximo        Minimo")
                for(DatosEstadisticosd : lista) {
                    out.printf("%-s          %d             %.2f               %.2f                 %.2f", f.nombreSerie(),f.numDatos(),f.media(),f.max(),f.min());
                }
            } catch (IOException e){
                System.out.println("Error al abrir "+nombreFichero);
            }
        }
    }
    */
}
